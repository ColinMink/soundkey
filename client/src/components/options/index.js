import React from 'react';
import './styles.css'

import OptHeader from './optHeader/index';
import OptContent from './optContent/index';
import OptFooter from './optFooter/index';

function Options(props) {
    return (
        /* Delete
        <div className='content'>
            {props.children}
        </div> */
            /*
            
            <OptHeader>
            <OptContent>
            <OptFooter>

            */
    );
  }
  
  export default Options;